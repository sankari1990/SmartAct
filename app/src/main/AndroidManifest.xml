<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smartretail">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".transactions.SalesEntryActivity"></activity>
        <activity android:name=".transactions.RateFixingActivity" />
        <activity android:name=".master.ItemMasterActivity" />
        <activity android:name=".master.GroupMasterActivity" />
        <activity android:name=".ReportActivity" />
        <activity android:name=".TransactionActivity" />
        <activity android:name=".MasterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".master.UnitMasterActivity" />
        <activity android:name=".master.PaymentMasterActivity" />
        <activity android:name=".master.TypeMasterActivity" />
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>